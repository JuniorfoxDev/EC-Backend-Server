{
  "builds": [
    {
       "src": "index.js", 
      "use": "@vercel/node", 
      "config": {
        "installCommand": "npm install --legacy-peer-deps"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.js" // Ensure this points to your entry point
    }
  ]
}
