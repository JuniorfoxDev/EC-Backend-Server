{
    "version": 2,
    "builds": [{
        "src": "index.js",
        "use": "@vercel/node",
        "command": "npm install --legacy-peer-deps && npm run build"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "/index.js"
    }]
}
